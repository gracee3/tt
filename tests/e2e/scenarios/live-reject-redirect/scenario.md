# Scenario: Live Reject Redirect

## Goal

Verify that Orcas can take one real live worker report, generate a real supervisor proposal from that report, and then let the operator redirect that proposal into a corrected bounded next assignment.

## What Is Seeded

1. A tiny git repository is materialized under the scenario worktree output root.
2. The repository contains one C source file with the same small greeting bug used by the other live scenarios.
3. A shell-based test script and Makefile are present so the repository can prove the fix locally.
4. The scenario writes a repo-local Orcas config that can target a local vLLM Responses endpoint by default, while still allowing an explicit hosted API override.
5. Orcas daemon state is started and a single workstream/work unit is created.
6. A tracked-thread record is created before the first live assignment, with a declared repository root, worktree path, branch name, base ref, landing target, and cleanup policy.
7. The harness inspects the workstream runtime before execution and expects zero live lane threads at that point.

## What Is Live

- The first worker turn is real and live.
- The report is generated by Orcas and persisted by Orcas.
- The supervisor proposal is generated from that persisted report by Orcas.
- The operator review step is real and uses the CLI redirect path against the persisted proposal.
- The next assignment is created by the daemon, not seeded by the harness.

## Live Boundary

- Before execution begins, the harness may create the fixture repository, workstream, work unit, tracked-thread workspace contract, and initial assignment.
- After `assignment/start` begins, the harness must not patch the source file, seed a report, seed a proposal, or seed the redirect result.
- The harness may inspect durable CLI-visible state, request a proposal from the daemon, redirect it through the CLI, and verify the resulting next assignment.

## What This Proves

- Orcas can finish a real bounded worker turn in the live lane.
- Orcas can bind the first live assignment directly into the predeclared tracked-thread workspace lane and surface it as one managed workstream-runtime thread.
- Orcas can persist the resulting report.
- Orcas can generate a reviewable supervisor proposal from that report.
- Operator redirect can override the proposal into a corrected bounded next assignment.
- The created next assignment remains bounded and linked to the same work unit.

## Pass Conditions

- The first live assignment reaches `AwaitingDecision`.
- The tracked-thread record exists before execution, then auto-binds to the live upstream thread.
- The workstream runtime shows exactly one managed lane thread and no unmanaged external thread after the first live assignment runs.
- A report exists and is linked to the assignment and work unit.
- The report parse result is acceptable for a real live run.
- `make test` passes in the fixture repository.
- A proposal is generated from the real persisted report.
- The proposal is open, reviewable, and linked to the work unit and source report.
- Redirect succeeds and produces a decision.
- Redirect creates a next assignment.
- The approved draft assignment reflects the operator redirect inputs.
- The next assignment is linked to the same work unit and remains bounded.

## Fail Conditions

- The first live assignment never reaches `AwaitingDecision`.
- The tracked-thread lane does not bind automatically or the runtime view is inconsistent with the declared lane.
- No report is persisted.
- Proposal generation fails or produces an unreviewable proposal.
- Redirect fails.
- No next assignment is created after redirect.
- The corrected assignment is not observably different from the original proposed draft assignment.
- The expected file churn is broader than the one-file code fix from the worker turn.
- The local fixture test still fails after the worker turn.

## Why Redirect Instead of Reject

Redirect is the stronger governed-path check in this product shape because it proves the operator can redirect a live proposal into a corrected bounded next assignment. A pure reject would prove the review gate, but not the corrected-assignment branch.

## Known Flake Points

- Live worker latency can vary.
- Live proposal generation depends on the supervisor backend and network availability.
- The scenario uses a repo-local config override so it can run against either a local vLLM endpoint or an explicitly configured hosted API.
- The daemon bootstrap may take a few seconds before the assignment starts.
- The `assignment/start` CLI background job can outlive the scenario even when the daemon completes the turn; the scenario validates persisted state instead of depending on that RPC to exit cleanly.

## Why This Stops at Corrected Assignment Creation

The purpose of this slice is to prove the governed branch of the live loop:

1. real report
2. real supervisor proposal
3. real operator redirect
4. real corrected next assignment creation

Executing the second assignment would add another live turn without proving any new boundary in this migration slice. That belongs in a later scenario if needed.
